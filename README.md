# trs-isomorphic-search
The system mathematically proves that a runtime graph is structurally identical to a trusted template. It ignores node names and focuses on edge connectivity and node attributes (layer_type).
